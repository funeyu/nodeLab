#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../nodemailer/bin/nodemailer" "$@"
  ret=$?
else 
  node  "$basedir/../nodemailer/bin/nodemailer" "$@"
  ret=$?
fi
exit $ret
